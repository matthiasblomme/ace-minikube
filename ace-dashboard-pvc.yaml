apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ace-dashboard-content
  namespace: ace-demo
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi